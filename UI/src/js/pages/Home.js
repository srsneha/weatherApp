var React = require('react');
var AppFrame = require('../components/AppFrame');

var Home = React.createClass({
  render () {
    return (
          <AppFrame />
    );
  }
});

module.exports = Home;
